type Props = {};

export default function Bookmark({}: Props) {
   return (
      <button className="h-full grid place-content-center">
         <span className="material-icons text-5xl">bookmark_border</span>
      </button>
   );
}
